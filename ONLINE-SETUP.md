# راه‌اندازی آنلاین درسیتا

## وضعیت پروژه
رابط کاربری، مسیریابی، ثبت فعالیت، اتاق‌ها، چت، احراز هویت و پنل مدیر در مخزن آماده شده‌اند. اتصال واقعی سرویس آنلاین فقط با ساخت پروژه Supabase و قرار دادن URL و anon key فعال می‌شود.

## 1) انتشار روی GitHub Pages
در GitHub وارد Settings → Pages شوید و Deploy from a branch را انتخاب کنید، سپس branch `main` و folder `/ (root)` را انتخاب کنید.

آدرس پیش‌فرض پروژه:
`https://hkhany753-hub.github.io/bh-bh/`

## 2) ساخت پروژه Supabase
یک پروژه در Supabase بسازید و در SQL Editor این فایل‌ها را به همین ترتیب اجرا کنید:

1. `supabase-schema.sql`
2. `admin-schema.sql`
3. `supabase-online-upgrade.sql`

فایل سوم ساخت خودکار پروفایل، عضویت اتاق و Realtime را تکمیل می‌کند.

## 3) اتصال فرانت‌اند
مقادیر Project URL و anon/public key را در `config.js` قرار دهید:

```js
window.APP_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-KEY',
  ADMIN_EMAIL: ''
};
```

`ADMIN_EMAIL` دیگر معیار امنیتی مدیر نیست؛ دسترسی واقعی بر اساس `profiles.role = 'admin'` در دیتابیس کنترل می‌شود. بنابراین service_role key را هرگز داخل GitHub یا `config.js` قرار ندهید.

## 4) مدیر اصلی
بعد از ساخت حساب خودت، UUID کاربر را از Auth/Database پیدا کن و فقط یک بار اجرا کن:

```sql
update public.profiles
set role = 'admin'
where id = 'YOUR_AUTH_USER_UUID';
```

بعد از ورود دوباره، گزینه «مدیریت سایت» فقط برای همان حساب و سایر حساب‌های دارای نقش admin نمایش داده می‌شود.

## 5) امکانات آنلاین
- ثبت‌نام و ورود با Supabase Auth
- ماندگاری جلسه بین بازدیدها
- پروفایل کاربر
- ذخیره فعالیت‌ها در دیتابیس
- ذخیره اتاق‌ها در دیتابیس
- عضویت پایدار در اتاق‌ها
- گفت‌وگوی عمومی و گفت‌وگوی اتاق با Realtime
- گزارش کاربران و اطلاعیه‌ها
- پنل مدیریت و تغییر نقش کاربران
- لاگ مدیریتی و RLS
- fallback محلی در صورت نبود تنظیمات Supabase

## نکته امنیتی
مدیر به رمز عبور کاربران دسترسی ندارد. بررسی نقش مدیر در رابط فقط برای UX است و مجوزهای اصلی با Row Level Security و تابع `is_admin()` در Supabase enforce می‌شوند.

## تست سریع
بعد از وارد کردن URL و anon key:

1. دو حساب بساز و با دو مرورگر وارد شو.
2. در یک مرورگر پیام بفرست و بازتاب آن را در دیگری بررسی کن.
3. یک فعالیت ثبت کن و رفرش انجام بده؛ فعالیت باید از دیتابیس برگردد.
4. برای حساب مدیر نقش `admin` بگذار و بخش «مدیریت سایت» را باز کن.
